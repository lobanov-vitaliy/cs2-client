'use client';

import { Next13ProgressBar } from 'next13-progressbar';

const NProgress = () => {
  return <Next13ProgressBar color="rgba(255,255,255,0.7)" options={{ showSpinner: false }} />
}

export default NProgress